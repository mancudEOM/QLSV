@model IEnumerable<QLSV.Models.Rent>

@{
    ViewData["Title"] = "History";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Semeter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDateRent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DueDateRent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Semeter)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDateRent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DueDateRent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
